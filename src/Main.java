import java.io.IOException;

public class Main {
    public static void main(String[] args) throws IOException {
        
        GameFrame gameFrame = new GameFrame();

        // if avec remise à 0 de la valeur, initialiser la valeur ici aussi

        /*TO BE DONE

        Ajouter deux boutons en Game Over :
        [Try again?] -- [Quit]
        - relance startGame();
        - ferme la fenêtre .dispose();

        Ajouter un sélecteur de vitesse en combobox sur GameOver
        avec codes difficulté pour vitesse différente (DELAY)
        afficher difficulté choisie sur écran du GAMEOVER

        peut-être plutôt faire DELAY++ à chaque pomme mangée?
         */
    }

}